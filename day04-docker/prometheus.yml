global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "fastapi"

    metrics_path: /metrics

    static_configs:
      - targets:
          - "api:8000"
          # 不能写localhost：8000
          #原因是Prometheus运行在自己的容器中。在Prometheus容器内部，
          #localhost代表Prometheus自己，不是FastAPI容器。